name = "temp-email-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.production]
name = "temp-email-worker"

[[d1_databases]]
binding = "DB"
database_name = "temp-email-db"
database_id = "e4877723-7458-48b4-9f49-80598e31f8ca"

[vars]
DOMAIN = "aihcolamail.xyz"

# 可选环境变量（仅当需要发送邮件功能时）
# 如果只需要接收邮件，可以不配置 RESEND_API_KEY
# RESEND_API_KEY = "your-resend-api-key"
